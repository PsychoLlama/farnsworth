import { createAction } from 'retreon';

export const open = createAction('settings/open');
export const close = createAction('settings/close');
export const toggle = createAction('settings/toggle');
